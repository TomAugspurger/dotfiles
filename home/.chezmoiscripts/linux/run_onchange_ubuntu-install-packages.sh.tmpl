{{ if eq .chezmoi.os "linux-ubuntu" -}}
#!/bin/bash

{{ $packages := list
     "build-essential"
     "curl"
     "fd-find"
     "fzf"
     "git"
     "jq"
     "ripgrep"
     "fish"
     "docker-buildx"
     "docker-compose-v2" -}}


{{ $sudo := "sudo " -}}
{{ if eq .chezmoi.username "root" -}}
{{   $sudo = "" -}}
{{ end -}}

#!/bin/bash

set -eufo pipefail

{{ $sudo }}apt-get update
{{ $sudo }}apt-get install -y {{ $packages | join " " }}

{{ end -}}
